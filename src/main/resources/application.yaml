spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: resilient-shop
      username: admin
      password: admin123
      authentication-database: admin


management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus